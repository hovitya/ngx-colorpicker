import { NgxColorDisplayDirective } from './ngx-color-display.directive';

describe('NgxColorDisplayDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxColorDisplayDirective();
    expect(directive).toBeTruthy();
  });
});
